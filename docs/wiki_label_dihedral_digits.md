# Label dihedral digits

= Overview =
label_dihedral_digits sets the precision of dihedral labels

= Usage =

```python
1. set dihedral labels to display 2 decimals places to the right of the period
set label_dihedral_digits, 2
```

= See Also =
- label_angle_digits
- label_distancel_digits
