# Mesh mode

##  Overview
set mesh_mode changes the mesh drawing mode for non-protein objects.

mesh_mode 1 allows you to show meshes for hetero atom and solvent atom objects.

##  Syntax
```python
set mesh_mode,                           #default 0
```
